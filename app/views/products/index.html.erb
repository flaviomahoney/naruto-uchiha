<div class="container">
    <% @products.each do |product| %>
        <%= product.name %>
        <br>
        <%= product.description %>
        <br>
        <%= product.size %>
        <br>
        <%= product.price %>
        <br>
        <br>
    <% end %>
</div>